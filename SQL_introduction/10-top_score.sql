-- This script lists all records from 'second_table', displaying 'score' and 'name'.
-- Records are ordered by 'score' in descending order (top first).

SELECT score, name FROM second_table ORDER BY score DESC;
